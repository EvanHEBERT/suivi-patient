[build]
  # On laisse "base" vide car le package.json est à la racine
  base = ""
  # Dossier généré par Vite
  publish = "dist"
  command = "npm run build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200